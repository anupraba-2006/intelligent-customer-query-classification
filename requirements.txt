pandas>=2.0   #  for Dataset read, clean, manage 
numpy>=1.24   #Numbers, arrays, mathematical operations
scikit-learn>=1.3   #ML model, TF-IDF, train/test split, accuracy
joblib>=1.3   #to save the trained model
fastapi>=0.110  # to expose the ML model into API
uvicorn[standard]>=0.29  # to run the fast API application
pydantic>=2.0  # to check the input format is correct
pytest>=8.0   #to check the code is work properly
httpx>=0.27   # to send the request to API
